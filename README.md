# GolmokGyeol-FE

React, Vite, TypeScript 기반의 골목결 프런트엔드 프로젝트입니다.

## 시작하기

```bash
npm install
npm run dev
```

개발 서버는 기본적으로 `http://localhost:5173`에서 실행됩니다.

## 명령어

```bash
npm run dev      # 개발 서버 실행
npm run build    # 타입 검사 및 프로덕션 빌드
npm run lint     # 정적 분석
npm run preview  # 프로덕션 빌드 미리보기
```
